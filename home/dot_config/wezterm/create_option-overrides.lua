local module = {}

-- Apply any desired overrides to the default configuration.
function module.apply_to_config(config)
	-- config.font_size = 10.0
	-- config.window_frame = {
	-- 	font_size = config.font_size,
	-- }
end

return module
